<%= turbo_frame_tag @try do %>
  
  <%= render 'shared/errors', object: @try %>

  <%= form_with model: @try do |form| %>
    <div>
      <%= form.label :result, "Ваш варіант" %><br>
      <%= form.text_field :result %>
    </div>
    <br>
    <div>
      <%= form.submit 'Спробувати', class: "btn btn-primary" %>
    </div>
  <% end %>
<% end %>